# Pi Monitor 가동 가이드

## 구성

```text
Vercel Vue
  → Cloudflare Quick Tunnel
  → Raspberry Pi Spring Boot
  → PostgreSQL

Raspberry Pi 센서 수집기
  → localhost:8080/api/sensors
```

Vercel 주소:

```text
https://monitor-ochre-three.vercel.app
```

## 1. 백엔드와 PostgreSQL 가동

Raspberry Pi에 SSH로 접속한 후 실행한다.

```bash
cd ~/apps/pi-monitor
./scripts/pi-compose.sh up -d --build
./scripts/pi-compose.sh ps
```

API 확인:

```bash
curl http://localhost:8080/api/sensors/latest
```

## 2. 센서 수집기 가동

```bash
sudo systemctl enable --now pi-monitor-sensor
sudo systemctl status pi-monitor-sensor --no-pager
```

센서 로그:

```bash
journalctl -u pi-monitor-sensor -f
```

로그 보기는 `Ctrl+C`로 종료해도 서비스가 계속 실행된다.

## 3. Quick Tunnel 최초 가동

PowerShell과 SSH를 종료해도 유지되도록 Docker 백그라운드에서 실행한다.

```bash
docker run -d \
  --name pi-monitor-quick-tunnel \
  --network host \
  --restart unless-stopped \
  cloudflare/cloudflared:latest \
  tunnel --url http://127.0.0.1:8080
```

생성된 주소를 확인한다.

```bash
docker logs -f pi-monitor-quick-tunnel
```

다음 형태의 주소를 확인한 뒤 `Ctrl+C`로 로그 보기를 종료한다.

```text
https://임의주소.trycloudflare.com
```

`Ctrl+C`는 로그 보기만 종료하며 Tunnel 컨테이너는 계속 실행된다.

## 4. 기존 Quick Tunnel 다시 가동

컨테이너가 이미 만들어져 있지만 중지된 경우:

```bash
docker start pi-monitor-quick-tunnel
docker logs -f pi-monitor-quick-tunnel
```

현재 상태 확인:

```bash
docker ps --filter name=pi-monitor-quick-tunnel
```

## 5. Vercel 환경변수 설정

Vercel 프로젝트의 `Settings → Environment Variables`에서 설정한다.

Quick Tunnel 주소가 `https://example.trycloudflare.com`이라면:

```text
VITE_API_URL=https://example.trycloudflare.com
VITE_WS_URL=wss://example.trycloudflare.com/ws
```

`Production`과 `Preview`에 적용하고 Vercel에서 `Redeploy`한다.

Quick Tunnel이 재시작되어 주소가 바뀌면 두 환경변수도 수정한 뒤 다시 배포해야 한다.

## 6. 전체 상태 확인

```bash
cd ~/apps/pi-monitor

./scripts/pi-compose.sh ps
systemctl is-active pi-monitor-sensor
docker ps --filter name=pi-monitor-quick-tunnel
curl http://localhost:8080/api/sensors/latest
```

외부 API 확인:

```bash
curl https://현재주소.trycloudflare.com/api/sensors/latest
```

정상 상태:

- PostgreSQL: `healthy`
- Spring Boot: `running`
- 센서 서비스: `active`
- Quick Tunnel: `Up`
- API: 최신 SensorData JSON 반환

## 7. 로그 확인

Spring Boot:

```bash
./scripts/pi-compose.sh logs -f backend
```

PostgreSQL:

```bash
./scripts/pi-compose.sh logs -f postgres
```

센서 수집기:

```bash
journalctl -u pi-monitor-sensor -f
```

Quick Tunnel:

```bash
docker logs -f pi-monitor-quick-tunnel
```

## 8. 안전하게 중지

Quick Tunnel만 중지:

```bash
docker stop pi-monitor-quick-tunnel
```

센서 수집기 중지:

```bash
sudo systemctl stop pi-monitor-sensor
```

백엔드와 PostgreSQL 중지:

```bash
cd ~/apps/pi-monitor
./scripts/pi-compose.sh stop
```

데이터를 보존하려면 `docker compose down -v`는 사용하지 않는다.

## 재부팅 후 빠른 점검

Docker 컨테이너와 센서 서비스는 자동 실행되도록 설정되어 있다. 재부팅 후 다음 명령만 확인한다.

```bash
cd ~/apps/pi-monitor
./scripts/pi-compose.sh ps
systemctl is-active pi-monitor-sensor
docker ps --filter name=pi-monitor-quick-tunnel
```

Quick Tunnel 주소가 변경되었다면 Vercel 환경변수를 수정하고 다시 배포한다.
