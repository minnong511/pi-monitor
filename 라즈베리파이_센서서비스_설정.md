# Raspberry Pi 센서 수집기 systemd 설정 및 문제 해결

## 목적

Python 센서 수집기를 Raspberry Pi 부팅 시 자동으로 실행하고, 실행 중 오류가 발생하면 자동으로 재시작한다.

프로젝트 위치 예시:

```text
/home/min/apps/pi-monitor
```

프로젝트 설치 위치가 다르면 아래 경로를 실제 경로로 변경해야 한다.

## 증상

센서 서비스 설치 중 다음 오류가 발생할 수 있다.

```text
Failed to start pi-monitor-sensor.service: Unit pi-monitor-sensor.service has a bad unit file setting.
```

검증 명령을 실행하면 다음과 같이 표시된다.

```bash
sudo systemd-analyze verify \
  /etc/systemd/system/pi-monitor-sensor.service
```

```text
WorkingDirectory= path is not absolute: "/home/min/apps/pi-monitor"
Unit configuration has fatal error, unit will not be started.
```

## 원인

1. `WorkingDirectory` 값의 따옴표를 현재 Raspberry Pi의 systemd가 경로 문자의 일부로 해석했다.
2. `newgrp docker`를 실행한 터미널에서 설치 스크립트를 실행하면 현재 기본 그룹이 `docker`로 감지될 수 있다.
3. 센서 수집기는 Docker 제어 권한이 필요하지 않으므로 서비스 그룹을 `docker`로 실행하면 불필요하게 높은 권한이 부여된다.

Python 가상환경과 `psutil` 설치 성공 여부는 이 오류와 관계없다.

## systemd 서비스 파일 수정

서비스 파일을 연다.

```bash
sudo nano /etc/systemd/system/pi-monitor-sensor.service
```

다음 내용으로 설정한다.

```ini
[Unit]
Description=Pi Monitor Sensor Collector
Wants=network-online.target
After=network-online.target docker.service

[Service]
Type=simple
User=min
Group=min
WorkingDirectory=/home/min/apps/pi-monitor
EnvironmentFile=-/etc/pi-monitor/sensor.env
Environment=PYTHONUNBUFFERED=1
ExecStart=/home/min/apps/pi-monitor/.venv/bin/python /home/min/apps/pi-monitor/raspberry/sensor_sender.py
Restart=always
RestartSec=5
TimeoutStopSec=15
NoNewPrivileges=true
PrivateTmp=true
UMask=0027

[Install]
WantedBy=multi-user.target
```

Nano에서는 `Ctrl+O`, `Enter`로 저장하고 `Ctrl+X`로 종료한다.

## 설정 적용 및 실행

systemd 설정을 다시 읽고 파일을 검증한다.

```bash
sudo systemctl daemon-reload

sudo systemd-analyze verify \
  /etc/systemd/system/pi-monitor-sensor.service
```

검증에 성공하면 아무것도 출력되지 않는다. 서비스를 실행하고 부팅 시 자동 실행되도록 설정한다.

```bash
sudo systemctl reset-failed pi-monitor-sensor
sudo systemctl enable --now pi-monitor-sensor
sudo systemctl status pi-monitor-sensor --no-pager -l
```

정상 상태:

```text
Active: active (running)
```

실시간 로그를 확인한다.

```bash
journalctl -u pi-monitor-sensor -f
```

`전송 및 저장 성공` 로그가 반복되면 정상이다. 로그 확인은 `Ctrl+C`로 종료한다.

## Python 수동 실행 확인

`.venv`는 실행 파일이 아니라 디렉터리이므로 경로 자체를 명령으로 실행할 수 없다.

```bash
conda deactivate
cd /home/min/apps/pi-monitor
source .venv/bin/activate
python raspberry/sensor_sender.py
```

수동 실행을 종료한 후 가상환경을 빠져나온다.

```bash
deactivate
```

운영 환경에서는 Python을 직접 실행하지 않고 systemd 서비스를 사용한다.

## 설치 스크립트 재발 방지 수정

`raspberry/install_sensor_service.sh`에서 현재 셸의 유효 그룹이 아니라 사용자 계정의 기본 그룹을 조회하도록 수정한다.

기존 코드:

```bash
SERVICE_GROUP="$(id -gn)"
```

수정 코드:

```bash
SERVICE_GROUP="$(id -gn "${SERVICE_USER}")"
```

서비스 파일 생성 부분의 경로 따옴표도 제거한다.

기존 코드:

```bash
"WorkingDirectory=\"${PROJECT_ROOT}\""
"ExecStart=\"${VENV_DIR}/bin/python\" \"${SENSOR_SCRIPT}\""
```

수정 코드:

```bash
"WorkingDirectory=${PROJECT_ROOT}"
"ExecStart=${VENV_DIR}/bin/python ${SENSOR_SCRIPT}"
```

수정 후 Bash 문법을 검사한다.

```bash
bash -n raspberry/install_sensor_service.sh
```

> 현재 설치 스크립트는 공백이 없는 프로젝트 경로를 기준으로 한다. 예: `/home/min/apps/pi-monitor`

## 최종 점검표

- [ ] `systemd-analyze verify`에서 오류가 발생하지 않는다.
- [ ] 서비스의 `User`와 `Group`이 모두 `min`이다.
- [ ] `WorkingDirectory`와 `ExecStart`가 절대경로다.
- [ ] `systemctl status`가 `active (running)`을 표시한다.
- [ ] `journalctl`에서 센서 데이터 전송 성공 로그가 확인된다.
- [ ] Raspberry Pi 재부팅 후에도 서비스가 자동 실행된다.

재부팅 후에는 다음 명령으로 확인한다.

```bash
sudo reboot
```

다시 SSH로 접속한 후:

```bash
systemctl is-enabled pi-monitor-sensor
systemctl is-active pi-monitor-sensor
curl http://localhost:8080/api/sensors/latest
```

정상 결과는 각각 `enabled`, `active`, 최신 센서 데이터 JSON이다.

## 프로젝트 업데이트 절차

개발 PC에서 변경사항을 GitHub의 `main` 브랜치에 병합한 후 Raspberry Pi에서 업데이트한다.

먼저 현재 상태와 적용 중인 커밋을 확인한다.

```bash
cd /home/min/apps/pi-monitor
git status --short
git log -1 --oneline
```

`git status --short`에 아무것도 출력되지 않으면 새로운 코드를 가져온다.

```bash
git pull --ff-only origin main
```

Spring Boot 백엔드를 다시 빌드하고 컨테이너를 갱신한다.

```bash
./scripts/pi-compose.sh up -d --build
```

Python 라이브러리를 갱신하고 센서 서비스를 재시작한다.

```bash
.venv/bin/python -m pip install -r raspberry/requirements.txt
sudo systemctl restart pi-monitor-sensor
```

일반적인 코드 업데이트에서는 `install_sensor_service.sh`를 다시 실행할 필요가 없다. systemd 서비스 내용이나 설치 경로가 변경된 경우에만 수정된 설치 스크립트를 다시 실행한다.

> `./scripts/pi-compose.sh up -d --build`는 PostgreSQL 데이터 볼륨을 유지한다. `docker compose down -v`는 데이터 볼륨을 삭제할 수 있으므로 사용하지 않는다.

## 업데이트 후 검증

컨테이너와 센서 서비스 상태를 확인한다.

```bash
./scripts/pi-compose.sh ps
sudo systemctl status pi-monitor-sensor --no-pager -l
curl http://localhost:8080/api/sensors/latest
```

다음 조건을 모두 만족해야 한다.

- [ ] PostgreSQL 컨테이너가 `healthy` 상태다.
- [ ] Spring Boot 백엔드 컨테이너가 실행 중이다.
- [ ] `pi-monitor-sensor`가 `active (running)` 상태다.
- [ ] 최신 데이터 API가 SensorData JSON을 반환한다.
- [ ] 센서 데이터가 PostgreSQL에 계속 추가된다.

## 업데이트 로그 확인

최근 적용된 Git 커밋을 확인한다.

```bash
git log -5 --date=iso --pretty=format:'%h %ad %an %s'
```

최근 10분 동안의 백엔드 로그를 확인한다.

```bash
./scripts/pi-compose.sh logs --since=10m backend
```

최근 10분 동안의 PostgreSQL 로그를 확인한다.

```bash
./scripts/pi-compose.sh logs --since=10m postgres
```

최근 10분 동안의 센서 서비스 로그를 확인한다.

```bash
journalctl \
  -u pi-monitor-sensor \
  --since '10 minutes ago' \
  --no-pager
```

센서 로그를 실시간으로 확인하려면 다음 명령을 사용한다.

```bash
journalctl -u pi-monitor-sensor -f
```

Raspberry Pi 패키지 업데이트 기록은 다음 파일에서 확인할 수 있다.

```bash
sudo less /var/log/apt/history.log
```

## 운영 업데이트 기록

업데이트할 때 아래 표에 적용한 커밋과 결과를 기록한다.

| 날짜 | 이전 커밋 | 적용 커밋 | 백엔드 | 센서 서비스 | API 확인 | 비고 |
| --- | --- | --- | --- | --- | --- | --- |
| YYYY-MM-DD HH:mm | `이전 커밋` | `적용 커밋` | 성공/실패 | 성공/실패 | 성공/실패 | 변경 내용 또는 오류 |

커밋 번호는 업데이트 전후에 다음 명령으로 확인한다.

```bash
git rev-parse --short HEAD
```

## Raspberry Pi OS 업데이트

프로젝트 업데이트와 별도로 운영체제 패키지를 갱신한다.

```bash
sudo apt update
apt list --upgradable
sudo apt full-upgrade -y
sudo reboot
```

재부팅 후 SSH로 다시 접속하여 프로젝트 상태를 확인한다.

```bash
cd /home/min/apps/pi-monitor
./scripts/pi-compose.sh ps
systemctl is-active pi-monitor-sensor
curl http://localhost:8080/api/sensors/latest
```
