Raspberry Pi
├─ sensor_sender.py
│      ↓ localhost
├─ Spring Boot :8080
│      ↓
└─ PostgreSQL

외부 브라우저
├─ Vercel Vue Dashboard
│      ↓ HTTPS / WSS
└─ Cloudflare Tunnel
       ↓
   Raspberry Pi Spring Boot

이러면 인터넷 끊겨도 일단은 라즈베리파이에서 계속 데이터 수집이 가능하다.

권장 외부 연결: Cloudflare Tunnel

https://api.example.com
        ↓
http://localhost:8080

# 권장 작업 순서

라즈베리파이에 PostgreSQL 설치
Spring Boot 백엔드 배포 및 systemd 등록
실제 센서 수집기 배포 및 systemd 등록
라즈베리파이 내부 localhost:8080 연동 확인
------
API 인증 추가
Cloudflare Tunnel로 api.example.com 연결
------
Spring CORS에 Vercel 주소 추가
Vercel에 VITE_API_URL, VITE_WS_URL 등록
------
Vue 프론트엔드 배포
재부팅·인터넷 단절·복구 테스트


# 1. 라즈베리파이에 파일 올리기

Docker Compose
├─ PostgreSQL
└─ Spring Boot Backend

Raspberry Pi 호스트
└─ sensor_sender.py → systemd

최종 구조

sensor_sender.py
    │ http://localhost:8080/api/sensors
    ▼
Spring Boot Container
    │ jdbc:postgresql://postgres:5432/pi_monitor
    ▼
PostgreSQL Container
    │
    ▼
Docker Volume

# 경로와 무관하게 설치하기

모든 스크립트는 자신의 파일 위치를 기준으로 프로젝트 루트를 계산한다.
프로젝트를 `/home/pi/pi-monitor`, `/opt/pi-monitor` 등 어느 경로에 두어도
설치할 수 있다. 프로젝트를 설치 후 다른 경로로 옮겼다면 설치 스크립트를
다시 실행해 systemd의 경로를 갱신한다.

## 1. Docker 환경변수 준비

프로젝트 루트에서 예제 파일을 복사하고 비밀번호를 변경한다.

```bash
cp .env.example .env
nano .env
```

`.env`는 Git에 올리지 않는다.

## 2. PostgreSQL과 Backend 실행

프로젝트 루트가 아닌 다른 현재 디렉터리에서도 실행 스크립트의 경로만
지정하면 된다.

```bash
./scripts/pi-compose.sh up -d --build
./scripts/pi-compose.sh ps
./scripts/pi-compose.sh logs -f backend
```

다른 디렉터리에서 실행하는 예:

```bash
/opt/pi-monitor/scripts/pi-compose.sh up -d --build
```

연결을 확인한다.

```bash
curl http://localhost:8080/api/sensors
```

## 3. Python과 센서 서비스 자동 설치

설치 스크립트는 일반 로그인 사용자로 실행한다. `sudo`로 스크립트 자체를
실행하지 않는다. 필요한 시스템 작업에서만 스크립트가 `sudo`를 사용한다.

```bash
./raspberry/install_sensor_service.sh
```

스크립트가 자동으로 처리하는 작업:

- Python 3, venv, pip 설치
- 프로젝트 루트의 `.venv` 생성
- `raspberry/requirements.txt` 라이브러리 설치
- `/etc/pi-monitor/sensor.env` 생성
- 현재 사용자와 실제 프로젝트 경로로 systemd 서비스 생성
- 부팅 자동 실행 설정 및 즉시 실행

Backend가 다른 서버에 있다면 설치할 때 URL을 전달한다.

```bash
PI_MONITOR_API_URL=http://192.168.0.10:8080/api/sensors \
./raspberry/install_sensor_service.sh
```

기존 `/etc/pi-monitor/sensor.env`도 교체하려면 다음처럼 실행한다.

```bash
PI_MONITOR_OVERWRITE_ENV=1 \
PI_MONITOR_API_URL=http://192.168.0.10:8080/api/sensors \
./raspberry/install_sensor_service.sh
```

상태와 로그를 확인한다.

```bash
sudo systemctl status pi-monitor-sensor
journalctl -u pi-monitor-sensor -f
```

## 4. 재부팅 후 확인

```bash
sudo systemctl enable --now docker
sudo reboot
```

재접속 후 확인한다.

```bash
./scripts/pi-compose.sh ps
sudo systemctl status pi-monitor-sensor
curl http://localhost:8080/api/sensors/latest
```

`docker compose down`은 컨테이너만 내리고 DB 볼륨은 유지한다.
`docker compose down -v`는 PostgreSQL 데이터 볼륨까지 삭제하므로 데이터
초기화 목적이 아니라면 사용하지 않는다.


# 권장 흐름 ---- 



# 1. 개발 PC에서 GitHub에 올리기

작업 브랜치

git switch -c feat/pi-deployment 

.env가 GitHub에 올라가지 않는지 확인

git check-ignore -v .env
git ls-files .env